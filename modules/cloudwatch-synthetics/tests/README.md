# CloudWatch Synthetics module tests

The tracked [basic/](./basic/) scenario packages neutral fixture source files
and verifies generic infrastructure. It contains no real endpoint, client code,
or service protocol.

Real canary code and integration tests must remain in the consumer's private
repository or ignored local workspace.
