# with-client-name

Dedicated test scenario for alarm description metadata coverage. This example keeps `tests/base` unchanged and exercises the root-module `client_name` input while exposing expected `account` and `source` metadata values as outputs.
